[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    regression: marks tests as ensuring that meet a previous benchmark

minversion = 6.0
addopts = "-ra -q -m 'not slow'"
testpaths =
    tests
    integration